# Functions

Created a program that adds two values and returns it

# Feature

Created a function that accepts two parameters, and it sums the values up, and returns the result which can be logged, or used in another variable

```js
function addPrices(a, b) {
  return a + b;
}
```

# Technologies

- Javascript
  -HTML
